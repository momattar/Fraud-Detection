@{
    ViewData["Title"] = "Prediction Result";
}

<h2>Fraud Detection Result</h2>

<div class="alert @(ViewBag.Result == "Fraud Detected" ? "alert-danger" : "alert-success")" role="alert">
    <h4>@ViewBag.Result</h4>
</div>

<a asp-action="Index" asp-controller="FraudController2" class="btn btn-primary">Test Another Transaction</a>
